// serves as an object of type GCompound of both the box and text for section 7

import acm.graphics.*;

public class sec7Box extends GCompound {

	public sec7Box(String name){
		GLabel label = new GLabel(name,50,50);
		add(label);
		add(new GLine())
	}
	
}
